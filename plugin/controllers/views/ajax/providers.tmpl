#from urllib import quote
#for $provider in $providers
	<a href="#" onclick="load_tvcontent('ajax/channels?id=$quote($provider[0])&stype=$stype'); return false">
		<img border='0' src='images/folder.png' alt='' />&nbsp;&nbsp;$provider[1]
	</a>
	<br />
#end for